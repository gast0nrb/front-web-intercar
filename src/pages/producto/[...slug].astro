---
import Layout from "../../layouts/Layout.astro"
import BigProduct from "../../components/product/BigProduct.astro"
import WrapCard from "../../components/WrapCard.astro"
const { slug } = Astro.params;

if(slug === undefined){
    return Astro.redirect('/404')
}

---

<Layout>
    <BigProduct url={`http://localhost:8080/api/intercar/products/${slug}`}>
    <WrapCard title="Productos similares" buttonTitle="Ver más"/>
</Layout> 
